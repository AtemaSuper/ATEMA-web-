const express = require("express");
const app = express();
// 一旦機能として非公開
// const contactBox = require('../service/contactBoxService')

/**
 * 通知画面のController
 */

// 一旦機能として非公開
// app.use('/',contactBox)

module.exports = app;
